# Copy to .env and fill in real values. Never commit the real .env file.

# Read by Settings today (app/core/config.py):
ENVIRONMENT=development

# Not read yet — reserved for later milestones (Supabase in Milestone 6,
# external API integrations in Milestones 12/16/18/20, notifications in
# Milestones 41-45). Listed here now so the full expected shape is documented
# up front; Settings will grow to read these as those milestones land.
DATABASE_URL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
FINNHUB_API_KEY=
FRED_API_KEY=
MARKETAUX_API_KEY=
OPENAI_API_KEY=
ADMIN_SECRET=
EXPO_ACCESS_TOKEN=
