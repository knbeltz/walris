# Copy to .env and fill in real values. Never commit the real .env file.

# Read by Settings today (app/core/config.py):
ENVIRONMENT=development
DATABASE_URL=
FMP_API_KEY=
MARKETAUX_API_KEY=
FRED_API_KEY=
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_JWKS_URL=
OPENAI_API_KEY=
ADMIN_SECRET=

# Not read yet — reserved for later milestones (Supabase URL/key filled in but
# unused since Milestone 6; notifications in Milestones 41-45). Listed here
# now so the full expected shape is documented up front; Settings will grow
# to read these as those milestones land.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
EXPO_ACCESS_TOKEN=
